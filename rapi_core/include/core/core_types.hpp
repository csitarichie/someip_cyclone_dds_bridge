#pragma once

namespace adst::ep::test_engine::core {

/**
 * Each CallBack is Identified in each port with a CallBackHandle
 * Gets assigned during listen by the port.
 */
using CallBackHandle = int;

} // namespace adst::ep::test_engine::core
